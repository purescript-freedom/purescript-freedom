{
  "private": true,
  "name": "basic",
  "scripts": {
    "clean": "rm -rf output",
    "spago": "spago",
    "start": "spago build && parcel ./public/index.html"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "parcel": "^1.12.3",
    "purescript": "^0.13.0",
    "spago": "^0.8.5"
  }
}
