{
  "private": true,
  "name": "svg",
  "scripts": {
    "postinstall": "spago install",
    "clean": "rm -rf output",
    "spago": "spago",
    "start": "spago build && parcel ./public/index.html"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "parcel": "^1.12.1",
    "purescript": "^0.12.3",
    "spago": "^0.7.0"
  }
}
