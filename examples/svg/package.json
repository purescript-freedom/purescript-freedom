{
  "private": true,
  "name": "svg",
  "scripts": {
    "clean": "rm -rf output",
    "spago": "spago",
    "start": "spago build && parcel ./public/index.html"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "parcel": "^1.12.3",
    "purs-bin-simple": "^0.3.0",
    "spago": "^0.8.5"
  }
}
