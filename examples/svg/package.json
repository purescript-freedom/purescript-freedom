{
  "private": true,
  "name": "svg",
  "scripts": {
    "clean": "rm -rf output",
    "spago": "spago",
    "start": "spago build && parcel ./public/index.html"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "parcel": "^1.12.3",
    "purescript": "^0.13.3",
    "spago": "^0.9.0"
  }
}
