{
  "private": true,
  "name": "svg",
  "scripts": {
    "clean": "rm -rf output",
    "spago": "spago",
    "start": "spago build && parcel ./public/index.html"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "parcel": "^1.12.4",
    "purescript": "^0.13.6",
    "spago": "^0.14.0"
  }
}
