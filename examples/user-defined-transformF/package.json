{
  "private": true,
  "name": "user-defined-transformF",
  "scripts": {
    "postinstall": "spago install",
    "clean": "rm -rf output",
    "spago": "spago",
    "start": "spago build && parcel ./public/index.html"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "parcel": "^1.12.3",
    "purescript": "^0.12.5",
    "spago": "^0.7.7"
  }
}
