{
  "private": true,
  "name": "user-defined-transformF",
  "scripts": {
    "clean": "rm -rf output",
    "spago": "spago",
    "start": "spago build && parcel ./public/index.html"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "parcel": "^1.12.3",
    "purescript": "^0.13.4",
    "spago": "^0.10.0"
  }
}
